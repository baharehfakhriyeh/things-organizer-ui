import React from 'react'
import ContainerBoard from '../components/container/ContainerBoard'

const MainBoard = () => {
  return (
    <div>
      <ContainerBoard parentId={null} />
    </div>
  )
}

export default MainBoard
